# PlannerStreamer runtime configuration

streamer:
  joystick_deadzone: 0.15
  yaw_speed: 1.5                  # rad/s at rx=1.0
  dt: 0.05                        # seconds per update tick
  controller_max_delta_speed: 0.3 # m/s max controller delta when nav is active
